# Jetson note:
# Prefer JetPack's system OpenCV build. The install script creates a venv with
# system site packages enabled so `import cv2` can use NVIDIA's OpenCV package.
numpy==1.26.4
psutil>=5.8.0
ultralytics>=8.3,<9

