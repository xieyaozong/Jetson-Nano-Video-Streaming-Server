# Sample Data

This directory is reserved for public, non-sensitive sample videos.

Large media files are intentionally not committed. Put local test clips here when
you want to run the server with `--source-type file`.

```bash
python -m server.main --source-type file --source sample_data/sample.mp4
```

